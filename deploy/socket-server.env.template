# ============================================================
# SOCKET SERVER ENV — Copy this to /var/www/vtab/socket-server/.env
# ============================================================

PORT=4001
NODE_ENV=production

# Allowed CORS origins (comma-separated)
SOCKET_ORIGINS=https://officehub360.vtabsquare.com,https://api.officehub360.vtabsquare.com

# Backend URL for chat module (internal — same server, so use localhost)
PY_API_BASE=http://127.0.0.1:5000/chat

# Backend URL for attendance module (internal)
BACKEND_URL=http://127.0.0.1:5000
